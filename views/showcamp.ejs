<% include partials/header %>

    <div class="container">
        <h1><%= camp.name %></h1>
        <img src="<%= camp.image %>" alt="imgage could not load">
        <p><%= camp.description %></p>
    </div>

<% include partials/footer %>